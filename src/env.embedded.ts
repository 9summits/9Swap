// Auto-generated at build time by ./build. Empty in source — the build
// script overwrites this file with the contents of `.env` immediately
// before `bun build --compile`, then restores it. At runtime these
// values fill `process.env` only for keys that are currently unset, so
// shell env and a `.env` in CWD still win when present.
export const EMBEDDED_ENV: Record<string, string> = {};
